package cmd

const DescSyncFlagConfig = `configuration file path, also set virtual cwd based on the configuration file directory, if not specify configuration file,
the behaviour is to look up "askep.config.yaml" in parents`
const DescSyncFlagsProfiles = "specify connection profile to use"
const DescSyncFlagsDry = "run without sync to the database and print the html output (concated with script & stylesheet)"
